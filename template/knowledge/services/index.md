# 服務總覽

> 導航用文件。每個服務只放一～兩行定位與關係，細節見各服務檔案。
> Agent：開發前先在此定位目標服務，再讀對應的 `<service>.md`。

| 服務 | 定位 | 細節 |
|---|---|---|
| (範例) service-a | 訂單核心服務，處理下單、庫存扣減；被 api-gateway 呼叫 | [service-a.md](./service-a.md) |

新增服務請使用 `ade-add-service` skill，或依 [_template.md](./_template.md) 手動建立並更新本表。
